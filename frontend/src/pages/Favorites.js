import React from 'react';
import { Container, Typography } from '@mui/material';

const Favorites = () => (
  <Container sx={{ mt: 4 }}><Typography variant="h4">Favorites - Coming Soon</Typography></Container>
);

export default Favorites;
